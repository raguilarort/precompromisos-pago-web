import { FiltrarCatalogoPipe } from './filtrar-catalogo-pipe';

describe('FiltrarCatalogoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarCatalogoPipe();
    expect(pipe).toBeTruthy();
  });
});
